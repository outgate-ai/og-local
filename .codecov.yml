coverage:
  status:
    patch:
      default:
        target: auto
        threshold: 0%
    project:
      default:
        target: auto
        threshold: 1%

ignore:
  - "cmd/ogl/**"
  - "internal/pii/onnx/**"

comment:
  layout: "diff,flags,files"
  behavior: default
  require_changes: true
